package com.drbrosdev.currencyconverter.ui.screens.exampleScreen

class DummyScreen {
    /*
        Here we define two composable functions
        1)In the first one we define all data we use, we define LaunchedEffect and
        reference the other composable, with all the properties needed
        2)Second screen is our "main screen". There we should reference all composable`s we
        define in "DummyScreenComponents"
     */
}